name: official-mq-smoke

on:
  pull_request:
    branches:
      - master
  merge_group:
    branches:
      - master
  workflow_dispatch:

jobs:
  official-mq-smoke:
    name: official-mq-smoke
    runs-on: ubuntu-latest
    steps:
      - name: Show trigger
        run: |
          echo "event=${GITHUB_EVENT_NAME}"
          echo "ref=${GITHUB_REF}"
          echo "sha=${GITHUB_SHA}"
